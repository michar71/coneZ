Known Firmware Bugs
===================
Identified 2026-02-05 via code review. Organized by severity.


CRITICAL
--------

#3  FIXED -- Mutex released before interp(); next_code copied to local buffer

#7  FIXED -- Duplicate "debug" global variable removed from main.cpp

#8  FIXED -- GPS_RX_PIN was a typo (40 instead of 44). LED_PIN=40 is correct.

#30 FIXED -- Duplicate geo function definitions (ODR violation).
    latlon_to_meters(), calculate_geo(), xy_to_polar() were defined in both
    basic_extensions.h and effects.cpp. Removed duplicates from header;
    also removed unused calculate_geo().

#31 FIXED -- setLEDr/g/b had no bounds checking on LED index.
    BASIC scripts with out-of-range indices could corrupt memory.
    Added bounds guards to all three functions.

#32 FIXED -- LUT globals (pLUT, lutSize, currentLUTIndex) not cleaned up
    between BASIC programs. Added cleanup in registerhook().


HIGH
----

#9  FIXED -- FastLED separated into dedicated render task (led.cpp/led.h).
    FastLED.show() now called from exactly one place: the LED render task
    on Core 1. All other code writes to LED buffers and calls led_show()
    to set a dirty flag. The render task runs at ~30 FPS.

#10 FIXED -- SOS_effect2() rewritten as non-blocking state machine.
    All delay() calls removed; uses millis()-based state machine
    (IDLE, WAIT_OFFSET, RAMP_UP, RAMP_DOWN) that returns to loop()
    each iteration. No new FreeRTOS task needed.

#11 FIXED -- Effects now use get_org_lat()/get_org_lon() instead of
    hardcoded origin coordinates. All three effects (SOS_effect, SOS_effect2,
    CIRCLE_effect) updated.

#12 FIXED -- SCALE_() returns input instead of scaled result

#13 FIXED -- TEMP/HUM function registration broken

#14 FIXED -- RGB<->HSV conversion functions are swapped

#15 FIXED -- ACCZ_() returns Y-axis instead of Z-axis

#16 FIXED -- EVENT_SYS_TIMER logic broken

#33 FIXED -- HAS*() functions had fragile fall-through logic.
    HASORIGIN_, HASGPS_, HASGYRO_, HASACC_, HASMAG_ rewritten with
    flat guard pattern to eliminate ambiguous fall-through.

#34 FIXED -- SETLEDRGB_() missing value clamp in non-gamma path.
    Array values were passed directly to LEDs without constraining to 0-255.

#35 FIXED -- FORMAT_() strncat buffer overflow.
    Third argument didn't account for existing buffer content.
    Also added bounds check on format argument pointer to prevent
    out-of-bounds stack reads when format has more specifiers than args.

#36 FIXED -- funhook_exec_() used unbounded sprintf().
    Replaced with snprintf() to prevent stack buffer overflow.

#37 FIXED -- WAIT_() busy-waited with yield() instead of vTaskDelay().
    Replaced with proper FreeRTOS delay to avoid burning 100% CPU.


MEDIUM
------

#18 FIXED -- SOURCE_SHELL debug flag corrected in printManager.cpp

#19 FIXED -- Added volatile to cross-core GPS/sensor/sun globals

#20 FIXED -- ROTATEARRAY_() left-rotate off-by-one corrected

#21 FIXED -- WAITFOR_() int8_t type mismatch corrected

#24 FIXED -- IMU_INT_PIN was a typo (37 instead of 41). RGB2_PIN=37 is correct.

#25 FIXED -- readFile() now reads line-by-line

#26 FIXED -- check_serial() only calls init_commands() once via flag

#27 FIXED -- Date/time functions now return -1 on error instead of 1

#28 FIXED -- DAY/MONTH/YEAR/GPSALT registered in funhook_(), GPSALT_T defined

#29 FIXED -- sunSetPosition() header signature matches implementation

#38 FIXED -- extern gps_pos_valid in effects.cpp missing volatile qualifier.

#39 FIXED -- Duplicate #include "basic_wrapper.h" in main.cpp.

#40 FIXED -- I2C_FREQ comment said 400 kHz but value was 100 kHz.

#41 FIXED -- threadLoopCount[] not volatile for cross-core access.

#42 FIXED -- GPS date/time accessors read TinyGPSPlus object cross-core.
    Cached day/month/year/hour/minute/second in volatile variables,
    updated in gps_loop(). All accessors now use cached values.

#43 FIXED -- basic.h/basic_extensions.h define globals and functions in headers.
    Added BASIC_WRAPPER_TU compile guard to basic.h ensuring it can only
    be included from basic_wrapper.cpp.

#44 FIXED -- main.cpp local hostname[16] shadowed unused static hostname[17].
    Removed the unused static global.

#45 FIXED -- vTaskDelay(1 / portTICK_PERIOD_MS) integer division yields 0
    if tick period > 1ms. Replaced all instances with pdMS_TO_TICKS()
    across main.cpp, basic.h, basic_wrapper.cpp, and led.cpp.

#46 FIXED -- Effects used Serial.print()/OutputStream directly, bypassing
    printManager mutex. Replaced with printfnl(SOURCE_OTHER, ...) calls
    in all three effects. Removed extern OutputStream from effects.cpp.

#47 FIXED -- sensors.cpp IMU structs (gValue, gyr, angle) not safe for
    cross-core reads. Cached acceleration values in volatile floats,
    updated in sensors_loop(). Accessors now use cached volatile copies.


LOW
---

#48 FIXED -- GPS get_speed()/get_dir() already use volatile globals
    (gps_speed, gps_dir). No change needed; confirmed correct.

#49 FIXED -- LittleFS mount failure not tracked.
    Added bool littlefs_mounted flag set on successful mount.
    Guarded startup.bas check and http FS page with flag.

#50 FIXED -- params[] array shared between BASIC task and main loop
    without volatile qualifier. Added volatile to declaration.

#51 FIXED -- Typos in identifiers and comments.
    IMU_avaliable -> IMU_available, imuAvaialble -> imuAvailable,
    "initalized" -> "initialized", "Calulate" -> "Calculate".
    Fixed across sensors.cpp, sensors.h, basic_wrapper.cpp,
    main.cpp, effects.cpp, basic_extensions.h.

#52 FIXED -- sensors_loop() read MPU6500 even when IMU init failed.
    Guarded accelerometer/gyro reads with IMU_available check.

#53 FIXED -- bat_voltage()/solar_voltage() returned half value on
    first call due to static last_val initialized to 0.
    Changed to -1 sentinel; first call now returns actual reading.

#54 FIXED -- getMaxAccXYZ() only tracked positive acceleration.
    Changed comparisons to use fabs() so negative values are
    correctly tracked by magnitude.


OPEN (won't fix / deferred)
---------------------------

#55 Magic numbers scattered throughout codebase.
    FreeRTOS stack sizes (65535), buffer sizes (128/255) used as raw
    literals instead of named constants. LED counts are now configurable
    via the [led] config section (led.count1..count4, default 50).

#56 Commented-out code in gps.cpp, effects.cpp, main.cpp, etc.
    Dead commented blocks left from development/debugging.

#57 set_basic_param/get_basic_param silently clamp invalid index
    to MAX_PARAMS-1 instead of rejecting out-of-range values.

#58 check_serial() 512-byte buffer has no overflow protection.

#59 SOS_effect()/CIRCLE_effect() still use blocking delay() calls.
    Kept intentionally for now.

#60 "help" shell command crashes the main thread.
    Root cause under investigation.

#79 cue.cpp CUE_TYPE_EFFECT and CUE_TYPE_GLOBAL dispatch are stubbed.
    Print "not yet implemented" and no-op. CUE_TYPE_EFFECT needs to
    launch the WASM at cue->effect_file with cue->params[16];
    CUE_TYPE_GLOBAL semantics aren't defined yet.


ROUND 3 FIXES (2026-04-19)
==========================

HIGH
----

#61 FIXED -- http.cpp page_catf() truncation corrupted HTML output.
    When vsnprintf overflowed the page_buf, n returned the full
    intended length so the guard `page_pos + n < sizeof(page_buf)`
    evaluated false and page_pos did not advance. The next
    page_catf call overwrote the truncated bytes, producing
    garbled HTML (visible on /dir and the partitions page). Fixed
    by clamping n to what was actually written and advancing
    page_pos unconditionally.

#62 FIXED -- cue.cpp elapsed_ms wrapped after ~49 days of playback.
    uint32_t (now_ms - music_start_ms) overflowed, causing cues
    to mis-match or stop firing during very long sessions.
    Lifted elapsed_ms and effective_start to 64-bit throughout
    the comparison.

#63 FIXED -- wasm_wrapper.cpp leaked host state on m3_RunStart failure.
    File slots, string pool, low heap, and wasm_current_path
    weren't reset on the m3_RunStart error path, so the next run
    inherited dirty state. Factored cleanup into
    wasm_cleanup_runtime() and called it from both the error and
    success paths.

#64 FIXED -- conez_wifi.cpp WiFi byte counters could tear on dual-core.
    volatile uint32_t s_tx_bytes/s_rx_bytes were load-add-stored
    from concurrent LWIP callbacks; a preempting core could lose
    an increment. Switched to std::atomic<uint32_t> with
    fetch_add(memory_order_relaxed).

MEDIUM
------

#65 FIXED -- c2wasm cw_malloc/realloc/calloc returned NULL on OOM.
    Many call sites used the result without checking. Wrapped both
    the embedded (cw_fatal + longjmp) and standalone (fprintf +
    exit) allocators to abort on NULL; callers are now guaranteed
    a valid pointer.

#66 FIXED -- c2wasm buf.c capacity doubling could overflow int.
    `while (nc < b->len + need) nc *= 2;` on a large-enough
    capacity wrapped to a negative value, causing an infinite
    loop or allocation failure. Added INT_MAX/2 guard that
    calls cw_fatal.

#67 FIXED -- WASM file_seek accepted any offset without validation.
    A negative or out-of-range offset could leave the FILE* in
    an undefined state. Now computes the target via ftell+fstat
    and rejects offsets that would move outside [0, filesize].

#68 FIXED -- psram.cpp had 8 unbounded `while (GPSPI2.cmd.X) ;` spins.
    If the SPI2 peripheral hung, the calling task looped forever.
    Added SPI2_WAIT(cond) macro (1M-iter cap ~= 4ms at 240MHz)
    and replaced all 8 spins.

#69 FIXED -- Telnet IAC subnegotiation could lock a client in state 3.
    A peer sending unterminated IAC SB payload would consume
    bytes indefinitely. Added iac_sb_bytes counter per client,
    aborted after 256 bytes.

#70 FIXED -- NMEA RMC/GGA overwrote lat/lon on bad-fix modes.
    RMC mode E/N/S or GGA quality 6-8 correctly left
    s_has_fix=false, but position was still copied to d->lat /
    d->lon. Consumers that forgot to check location_valid saw
    garbage coordinates (e.g. from dead-reckoning drift).
    Now only overwrite lat/lon when s_has_fix is true; the
    last known good position is preserved.

#71 FIXED -- datetime_to_epoch_ms only validated month.
    Garbage day/hour/minute/second in NMEA produced wildly
    wrong epochs. Now range-checks year (1970-2099), month
    (1-12), day (1-31), hour (0-23), minute (0-59), second
    (0-60 for leap); returns 0 on invalid input.

#72 FIXED -- config_save and config_set_field had no locking.
    A cross-task reader could observe a half-written string
    field (wifi_ssid, mqtt_broker, etc.). Added config_mutex
    created in config_init; take/give around set_field (per
    write) and save (snapshot loop).

#73 FIXED -- editor_save was not atomic.
    A crash or power loss mid-save left the file truncated or
    empty. Now writes to <path>.tmp, flushes+closes, then
    renames into place; unlinks the tmp on any error.

LOW
---

#74 FIXED -- led_setup() could leak CRGB arrays on re-entry.
    Safe today (called only from main setup()), but a future
    config-reload path would leak the four buffers. Now
    delete[] the old arrays before re-allocating; rmt_init()
    guarded by a static bool so RMT channels and encoder can't
    double-create.

#75 FIXED -- led_push_hw's grb_buf was shared without synchronization.
    Only one caller today, but led_show_now() is public.
    Extended led_mutex to cover the whole led_push_hw, serializing
    against led_set_channel writers and any hypothetical
    concurrent pusher. rmt_transmit reads grb_buf asynchronously
    until rmt_tx_wait_all_done returns, so the buffer must stay
    intact for the full duration.

#76 FIXED -- WASM string pool and low heap fragmented over time.
    pool_free/low_heap_free only reclaimed the top allocation;
    adjacent freed blocks weren't coalesced. Long-running modules
    gradually exhausted the pool even when total free space was
    ample. Now coalesce forward and backward on free, shrink
    bump when the coalesced block is at the top, and prune
    trailing dead records (marked size=0, skipped by first-fit
    since min alloc is 4 bytes).

#77 FIXED -- printManager ANSI stripper only handled CSI.
    `ESC ]` (OSC — hyperlinks, window titles) and single-char
    ESC sequences (`ESC 7`, `ESC c`, etc.) landed in the log
    ring buffer / file sink / MQTT feed mangled. State machine
    rewritten to handle ESC [ ... final (CSI), ESC ] ... BEL|ST
    (OSC), and ESC X (single-char intermediate).

#78 FIXED -- effects.cpp function-scope statics persisted across switches.
    Transitioning between effects (or calling one after a long
    gap) left stale prev_sec / state / target_ms / step / offset_ms
    values that could trigger a spurious fire or skip. Moved all
    statics to module scope with s_* prefixes and added
    effects_reset_all() (declared in effects.h) to zero them.


ROUND 3 NEW FEATURES (2026-04-19)
=================================

/nvs HTTP handler now renders NVS contents as HTML — grouped by
    namespace, with typed values (i8..i64, u8..u64, str, blob),
    HTML-escaped strings, and hex dumps for blobs (first 32 bytes
    with "... (N more)" suffix for the tail). Previously returned
    literal "FIXME..." text.
