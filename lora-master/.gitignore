# Firmware images distributed by the master node (build artifacts, not source)
firmware/**/*.bin

# Python bytecode cache
__pycache__/
*.pyc

# Live master config (ship conez-master.ini.example; the real one is per-deployment)
conez-master.ini
